Compile the TypeScript code and report any errors. If compilation succeeds, tell me the exact steps to test it:

1. Run `npm run compile`
2. If there are errors, fix them one at a time
3. Once it compiles, remind me to press F5 to launch the Extension Development Host
4. List what to verify: status bar item, hover tooltip, click refresh, error states
